syntax = "proto3";

import "google/protobuf/descriptor.proto";

package api_spec_tmpl.dsl;


// TODO: Определить политики доступа
// TODO: Определить передачу обязательных параметров пользователя в UserData, Scope?
// TODO: Определить аутентификацию, авторизацию
// TODO: прописать работу с клаймами в запросе, апикей

// Опция указывает на поле, которое содержит шаблон спецификации АПИ
extend google.protobuf.FieldOptions {
  string specification = 3010;
}
// Опции шаблона спецификации АПИ.
// Если шаблон содержит такие же поля, то значения опций шаблона будут переопределены значениями полей
// Если нужно дать возможность переопределить значение через опции сущности, то добавляем поле в шаблон.
// Если нужно задавать значение только через опции сущности, то добавляем поле в шаблон, а опцию в шаблоне не означиваем.
// Если не нужно давать возможность переопределить значение через опции сущности, то оставляем знаячения только в опциях шаблона
extend google.protobuf.MessageOptions {
  // Имя поля шаблона АПИ, которое содержит шаблоны сервисов спецификации АПИ.
  // Если не указано, берем поле service_set
  string service_set_field_name = 3001;
  // Комментарий файла спецификации АПИ
  string leading_comment = 3002;
}


