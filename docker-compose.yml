services:
  app:
    image: entity-house
    build:
      context: .
    environment:
      PROTO_PATH: /templates
      PROTO_OUT_PATH: /gen_out
      PROTO_IMPORT_PATH: .;/proto_deps
    volumes:
      - ./templates:/templates
      - ./gen_out:/gen_out
#    command: ["sleep","600"]
